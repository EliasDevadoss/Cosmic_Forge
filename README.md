# Cosmic Forge

A standalone WebGL visual synthesizer built with React, Vite, and Three.js.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The app includes shader particles, bloom, animated orbital ribbons, live presets, palette controls, and fullscreen mode.
